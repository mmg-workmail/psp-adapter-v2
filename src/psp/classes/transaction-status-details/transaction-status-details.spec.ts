import { TransactionStatusDetails } from './transaction-status-details';

describe('TransactionStatusDetails', () => {
  it('should be defined', () => {
    expect(new TransactionStatusDetails()).toBeDefined();
  });
});
